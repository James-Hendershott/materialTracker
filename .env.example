# Environment Configuration Template
# Copy this to .env.local (for mobile app) and .env (for server)

# Database Selection (postgres | mongodb | mariadb)
DATABASE_TYPE=postgres

# PostgreSQL Configuration
POSTGRES_HOST=your-unraid-server.local
POSTGRES_PORT=5432
POSTGRES_DB=materialtracker
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password

# MongoDB Configuration (alternative)
# MONGODB_URI=mongodb://your-unraid-server.local:27017/materialtracker

# MariaDB/MySQL Configuration (alternative)
# MARIADB_HOST=your-unraid-server.local
# MARIADB_PORT=3306
# MARIADB_DB=materialtracker
# MARIADB_USER=your_username
# MARIADB_PASSWORD=your_password

# Image Storage
IMAGE_UPLOAD_URL=http://your-unraid-server.local:3001/upload
IMAGE_BASE_URL=http://your-unraid-server.local:3001/images

# Server Configuration (for server.ts)
PORT=3001
UPLOAD_DIR=./uploads

# Security Settings
# Generate a strong random API key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=your-secure-api-key-here

# CORS: Comma-separated list of allowed origins (leave empty to allow all - NOT recommended for production)
# Example: http://localhost:8081,http://192.168.1.100:8081,https://yourdomain.com
ALLOWED_ORIGINS=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
